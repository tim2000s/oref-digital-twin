# settings

Settings ingestion. Screenshots (vision → typed schema → validate → confirm) and the optional client-side encrypted-prefs decrypt (WebCrypto PBKDF2 + AES-GCM; master password never leaves the device). Profile-side settings come from ingestion/ via Nightscout.

See [../DESIGN.md](../DESIGN.md) §5.2. Not implemented yet — scaffold only.
