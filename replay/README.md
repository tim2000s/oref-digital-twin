# replay

The oref `determine-basal` replay oracle. Runs the real oref0 / Trio controller (pinned per user-variant) over logged inputs to price how a settings change flips a decision. Decision-level counterfactual only — never a BG counterfactual. Do not reimplement determine-basal.

See [../DESIGN.md](../DESIGN.md) §2. Not implemented yet — scaffold only.
